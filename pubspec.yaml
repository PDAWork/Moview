name: movie
description: "Тестовое задание AppFox"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^13.2.0
  dio:
  flutter_dotenv:
  get_it: ^7.6.7
  flutter_bloc: ^8.1.4
  pretty_dio_logger: ^1.3.1
  dartz: ^0.10.1
  equatable: ^2.0.5
  drift: ^2.16.0
  sqlite3_flutter_libs: ^0.5.20
  path_provider: ^2.1.2
  path: ^1.9.0
  intl: ^0.19.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  drift_dev: ^2.16.0
  build_runner: ^2.4.8

flutter:
  uses-material-design: true

  assets:
    - .env
